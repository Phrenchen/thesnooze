import { WidgetUrlPipePipe } from './widget-url-pipe.pipe';

describe('WidgetUrlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WidgetUrlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
